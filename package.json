{
  "name": "trackerview",
  "version": "0.1.0",
  "private": true,
  "description": "Web app for parsing tracker release pages into a table view",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "start:local": "node src/server.js",
    "check:live": "bash scripts/check-live.sh",
    "dev:battle": "bash scripts/dev-with-check.sh"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "iconv-lite": "^0.6.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
